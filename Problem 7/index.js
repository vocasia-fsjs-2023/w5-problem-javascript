console.log("OUTPUT: ");
console.log("-------------------");

for (let r  = 1 ; r  <= 100; r += 3) {
    if (r % 2 === 0) {
        console.log(`${r} - genap`);
    }else {
        console.log(`${r} - ganjil`);
    }
    
}

console.log("OUTPUT: ");
console.log("------------------");

for (let o = 50; o <= 200; o += 5) {
    if (o % 3 === 0) {
        console.log(`${o} - faktor 3`);
    }else {
        console.log(`${o} - tidak bisa dibagi 3`)
    }
    
}

console.log("OUTPUT: ");
console.log("------------------");

for (let m = 100; m <= 200; m += 7) {
    if (m % 8 === 0) {
        console.log(` ${m}`)
    } 
        
       
}