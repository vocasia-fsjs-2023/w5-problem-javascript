console.log("OUTPUT:");
console.log("----------------");

let set = 1;
while (set <= 100) {
    if (set % 2 === 0) {
        console.log(`${set} - genap`);
    } else {
        console.log(`${set} - ganjil`);
    }
    set += 3;
}

console.log("----------------");

console.log("OUTPUT: ");
console.log("--------------------");

let a = 5;
while (a <= 200) {
    if (a % 3 === 0) {
        console.log(`${a} Bisa dibagi 3 `);
    } else {
        console.log(`${a} Tidak bisa dibagi 3`);
    }
    a += 5;
}

console.log("----------------");

console.log("OUTPUT: ");
console.log("----------------");

let c = 100;
while (c <= 200) {
    if (c % 8 === 0) {
        console.log(" " + c);
    }
    c += 7;
}

console.log("----------------");
